# Only needed if you self-host the optional Express backend (server/server.js)
# instead of deploying to Netlify. Copy this file to .env and fill in real values.

PORT=3000
ADMIN_USER=admin
ADMIN_PASS=change-me-to-something-strong
